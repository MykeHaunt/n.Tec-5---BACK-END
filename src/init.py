# src/init.py

# Define package-level variables
PACKAGE_NAME = "n.Tec-5"
VERSION = "1.0.0"

# Define a function to initialize the package
def initialize():
    print(f"Initializing {PACKAGE_NAME} version {VERSION}")

# Call the initialize function when the module is imported
initialize()
